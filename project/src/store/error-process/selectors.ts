import {NameSpace} from '../../const';
import {State} from '../../types/state';

// export const setError = (state: State):string | null => state[NameSpace.Error].error;
// export const clearError = (state: State):string | null => state[NameSpace.Error].error = null;
